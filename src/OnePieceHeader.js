export default function OnepieceHeader({ title }) {
    return (
        <header class="site-header">
            <div class="inner">
                <h1>☠️ {title} 맴버소개</h1>
            </div>
        </header>
    );
}